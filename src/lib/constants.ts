// Shared MVP constants.

/**
 * The "as of" date for all static sample data in the MVP. Every panel that
 * shows a freshness chip references this single source of truth, so when the
 * sample data is regenerated there is one place to bump. Phase 4 replaces this
 * with per-source live timestamps.
 */
export const DATA_AS_OF = '2025-06-17'
